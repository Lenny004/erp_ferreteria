<UserControl
    x:Class="Ferreteria.PuntoVenta.Views.Inventario.ProveedoresView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helpers="clr-namespace:Ferreteria.PuntoVenta.Helpers">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="/Resources/Styles/AppStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="380" />
        </Grid.ColumnDefinitions>

        <!-- Lista -->
        <Grid Grid.Column="0" Margin="0,0,16,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Margin="0,0,0,12">
                <TextBlock Text="Proveedores" Style="{StaticResource AppPanelTitleStyle}" />
                <TextBlock Text="Gestione los proveedores de la ferreteria" Style="{StaticResource AppPanelSubtitleStyle}" />
            </StackPanel>
            <Grid Grid.Row="1" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="120" />
                </Grid.ColumnDefinitions>
                <TextBox
                    x:Name="SearchTextBox"
                    Style="{StaticResource AppFieldTextBoxStyle}"
                    helpers:WatermarkService.Watermark="Buscar por nombre o NIT"
                    TextChanged="OnSearchChanged" />
                <Button
                    Grid.Column="1"
                    Margin="10,0,0,0"
                    Style="{StaticResource AppSecondaryWideButtonStyle}"
                    Click="OnNuevoClick">
                    <TextBlock Text="NUEVO" Foreground="{StaticResource AppBlanco}" FontWeight="Bold" />
                </Button>
            </Grid>
            <Border
                Grid.Row="2"
                Background="{StaticResource AppAzulMedio}"
                BorderBrush="{StaticResource AppAzulClaro}"
                BorderThickness="1"
                CornerRadius="{StaticResource AppRadius_MD}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Style="{StaticResource AppGridHeaderStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="130" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="NOMBRE" Style="{StaticResource AppGridHeaderTextStyle}" />
                        <TextBlock Grid.Column="1" Text="NIT" Style="{StaticResource AppGridHeaderTextStyle}" />
                        <TextBlock Grid.Column="2" Text="TELEFONO" Style="{StaticResource AppGridHeaderTextStyle}" />
                    </Grid>
                    <Grid Grid.Row="1">
                        <TextBlock
                            x:Name="EmptyStateText"
                            Text="Sin proveedores para mostrar."
                            Style="{StaticResource AppTableCellStyle}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed" />
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="ItemsList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource AppDataGridRowStyle}"
                                                Cursor="Hand"
                                                MouseLeftButtonUp="OnRowClick"
                                                Tag="{Binding Id}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="120" />
                                                    <ColumnDefinition Width="130" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding Name}" Style="{StaticResource AppTableCellStyle}" />
                                                <TextBlock Grid.Column="1" Text="{Binding Nit}" Style="{StaticResource AppTableCellStyle}" />
                                                <TextBlock Grid.Column="2" Text="{Binding Phone}" Style="{StaticResource AppTableCellStyle}" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- Formulario -->
        <Border
            Grid.Column="1"
            Background="{StaticResource AppAzulMedio}"
            BorderBrush="{StaticResource AppAzulClaro}"
            BorderThickness="1"
            CornerRadius="{StaticResource AppRadius_MD}"
            Padding="16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock x:Name="FormTitle" Text="Nuevo proveedor" Style="{StaticResource AppPanelTitleStyle}" Margin="0,0,0,12" />

                    <TextBlock Text="Nombre / Razon social *" Style="{StaticResource AppCardLabelStyle}" />
                    <TextBox x:Name="NameBox" Style="{StaticResource AppFieldTextBoxStyle}" Margin="0,4,0,10" />

                    <TextBlock Text="Nombre comercial" Style="{StaticResource AppCardLabelStyle}" />
                    <TextBox x:Name="TradeNameBox" Style="{StaticResource AppFieldTextBoxStyle}" Margin="0,4,0,10" />

                    <TextBlock Text="NIT" Style="{StaticResource AppCardLabelStyle}" />
                    <TextBox x:Name="NitBox" Style="{StaticResource AppFieldTextBoxStyle}" Margin="0,4,0,10" />

                    <TextBlock Text="NRC" Style="{StaticResource AppCardLabelStyle}" />
                    <TextBox x:Name="NrcBox" Style="{StaticResource AppFieldTextBoxStyle}" Margin="0,4,0,10" />

                    <TextBlock Text="Contacto" Style="{StaticResource AppCardLabelStyle}" />
                    <TextBox x:Name="ContactBox" Style="{StaticResource AppFieldTextBoxStyle}" Margin="0,4,0,10" />

                    <TextBlock Text="Telefono" Style="{StaticResource AppCardLabelStyle}" />
                    <TextBox x:Name="PhoneBox" Style="{StaticResource AppFieldTextBoxStyle}" Margin="0,4,0,10" />

                    <TextBlock Text="Correo" Style="{StaticResource AppCardLabelStyle}" />
                    <TextBox x:Name="EmailBox" Style="{StaticResource AppFieldTextBoxStyle}" Margin="0,4,0,10" />

                    <TextBlock Text="Direccion" Style="{StaticResource AppCardLabelStyle}" />
                    <TextBox x:Name="AddressBox" Style="{StaticResource AppFieldTextBoxStyle}" Margin="0,4,0,10" />

                    <TextBlock Text="Dias de credito" Style="{StaticResource AppCardLabelStyle}" />
                    <TextBox x:Name="CreditDaysBox" Text="0" Style="{StaticResource AppFieldTextBoxStyle}" Margin="0,4,0,10" />

                    <TextBlock Text="Notas" Style="{StaticResource AppCardLabelStyle}" />
                    <TextBox x:Name="NotesBox" Style="{StaticResource AppFieldTextBoxStyle}"
                             TextWrapping="Wrap" AcceptsReturn="True" Height="60" Margin="0,4,0,10" />

                    <TextBlock x:Name="FormErrorText" Foreground="{StaticResource AppError}"
                               TextWrapping="Wrap" Visibility="Collapsed" Margin="0,0,0,8" />

                    <Grid Margin="0,4,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button Style="{StaticResource AppPrimaryWideButtonStyle}" Click="OnGuardarClick" Margin="0,0,6,0">
                            <TextBlock Text="GUARDAR" Foreground="{StaticResource AppBlanco}" FontWeight="Bold" />
                        </Button>
                        <Button Grid.Column="1" Style="{StaticResource AppSecondaryWideButtonStyle}" Click="OnCancelarClick" Margin="6,0,0,0">
                            <TextBlock Text="LIMPIAR" Foreground="{StaticResource AppBlanco}" FontWeight="Bold" />
                        </Button>
                    </Grid>
                    <Button x:Name="DeactivateButton" Style="{StaticResource AppSecondaryWideButtonStyle}"
                            Click="OnDesactivarClick" Margin="0,10,0,0" Visibility="Collapsed">
                        <TextBlock Text="DESACTIVAR" Foreground="{StaticResource AppError}" FontWeight="Bold" />
                    </Button>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
