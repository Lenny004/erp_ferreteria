using System.Windows;

namespace Ferreteria.PuntoVenta
{
    /// <summary>
    /// Ventana plantilla generada por WPF (no forma parte del flujo operativo).
    /// El arranque real usa <see cref="Views.Inicio.InicioWindow"/> → PIN → <see cref="Views.Shell.MainShellWindow"/>.
    /// </summary>
    public partial class MainWindow : Window
    {
        /// <summary>
        /// Inicializa la ventana plantilla. Preferir InicioWindow como entrada de la app.
        /// </summary>
        public MainWindow()
        {
            InitializeComponent();
        }
    }
}
