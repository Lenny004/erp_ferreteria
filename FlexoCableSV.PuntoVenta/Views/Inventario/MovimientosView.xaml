<UserControl
    x:Class="FlexoCableSV.PuntoVenta.Views.Inventario.MovimientosView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helpers="clr-namespace:FlexoCableSV.PuntoVenta.Helpers">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="/Resources/Styles/FlexoStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="360" />
        </Grid.ColumnDefinitions>

        <!-- Kardex -->
        <Grid Grid.Column="0" Margin="0,0,16,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Margin="0,0,0,12">
                <TextBlock Text="Entradas y Kardex" Style="{StaticResource FlexoPanelTitleStyle}" />
                <TextBlock Text="Movimientos de inventario recientes" Style="{StaticResource FlexoPanelSubtitleStyle}" />
            </StackPanel>
            <Button Grid.Row="1" HorizontalAlignment="Left" Style="{StaticResource FlexoSecondaryWideButtonStyle}"
                    Click="OnActualizarClick" Margin="0,0,0,12">
                <TextBlock Text="ACTUALIZAR" Foreground="{StaticResource FlexoBlanco}" FontWeight="Bold" />
            </Button>
            <Border
                Grid.Row="2"
                Background="{StaticResource FlexoAzulMedio}"
                BorderBrush="{StaticResource FlexoAzulClaro}"
                BorderThickness="1"
                CornerRadius="{StaticResource FlexoRadius_MD}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Style="{StaticResource FlexoGridHeaderStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="130" />
                            <ColumnDefinition Width="110" />
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="80" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="FECHA" Style="{StaticResource FlexoGridHeaderTextStyle}" />
                        <TextBlock Grid.Column="1" Text="CODIGO" Style="{StaticResource FlexoGridHeaderTextStyle}" />
                        <TextBlock Grid.Column="2" Text="TIPO" Style="{StaticResource FlexoGridHeaderTextStyle}" />
                        <TextBlock Grid.Column="3" Text="PRODUCTO" Style="{StaticResource FlexoGridHeaderTextStyle}" />
                        <TextBlock Grid.Column="4" Text="CANT." Style="{StaticResource FlexoGridHeaderTextStyle}" />
                        <TextBlock Grid.Column="5" Text="SALDO" Style="{StaticResource FlexoGridHeaderTextStyle}" />
                    </Grid>
                    <Grid Grid.Row="1">
                        <TextBlock
                            x:Name="EmptyStateText"
                            Text="Sin movimientos para mostrar."
                            Style="{StaticResource FlexoTableCellStyle}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed" />
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="ItemsList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource FlexoDataGridRowStyle}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="130" />
                                                    <ColumnDefinition Width="110" />
                                                    <ColumnDefinition Width="140" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="80" />
                                                    <ColumnDefinition Width="80" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding CreatedAt, StringFormat='dd/MM HH:mm'}" Style="{StaticResource FlexoTableCellStyle}" />
                                                <TextBlock Grid.Column="1" Text="{Binding ProductCode}" Style="{StaticResource FlexoTableCellStyle}" Foreground="{StaticResource FlexoRojo}" />
                                                <TextBlock Grid.Column="2" Text="{Binding MovementType}" Style="{StaticResource FlexoTableCellStyle}" />
                                                <TextBlock Grid.Column="3" Text="{Binding ProductDescription}" Style="{StaticResource FlexoTableCellStyle}" />
                                                <TextBlock Grid.Column="4" Text="{Binding Quantity}" Style="{StaticResource FlexoTableCellStyle}" />
                                                <TextBlock Grid.Column="5" Text="{Binding StockAfter}" Style="{StaticResource FlexoTableCellStyle}" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- Formulario de movimiento -->
        <Border
            Grid.Column="1"
            Background="{StaticResource FlexoAzulMedio}"
            BorderBrush="{StaticResource FlexoAzulClaro}"
            BorderThickness="1"
            CornerRadius="{StaticResource FlexoRadius_MD}"
            Padding="16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Registrar movimiento" Style="{StaticResource FlexoPanelTitleStyle}" Margin="0,0,0,12" />

                    <TextBlock Text="Producto *" Style="{StaticResource FlexoCardLabelStyle}" />
                    <ComboBox x:Name="ProductCombo" Style="{StaticResource FlexoFieldComboBoxStyle}"
                              DisplayMemberPath="Display" SelectedValuePath="Id"
                              IsEditable="True" IsTextSearchEnabled="True"
                              SelectionChanged="OnProductSelected" Margin="0,4,0,10" />

                    <TextBlock x:Name="CurrentStockText" Text="Stock actual: -"
                               Foreground="{StaticResource FlexoInfo}" Margin="0,0,0,10" />

                    <TextBlock Text="Tipo de movimiento *" Style="{StaticResource FlexoCardLabelStyle}" />
                    <ComboBox x:Name="TypeCombo" Style="{StaticResource FlexoFieldComboBoxStyle}"
                              SelectedIndex="0" SelectionChanged="OnTypeChanged" Margin="0,4,0,10">
                        <ComboBoxItem Content="Entrada por compra" Tag="ENTRADA_COMPRA" />
                        <ComboBoxItem Content="Entrada por devolucion" Tag="ENTRADA_DEVOLUCION" />
                        <ComboBoxItem Content="Ajuste (fijar stock)" Tag="AJUSTE" />
                    </ComboBox>

                    <TextBlock x:Name="QuantityLabel" Text="Cantidad a ingresar *" Style="{StaticResource FlexoCardLabelStyle}" />
                    <TextBox x:Name="QuantityBox" Text="0" Style="{StaticResource FlexoFieldTextBoxStyle}" Margin="0,4,0,10" />

                    <StackPanel x:Name="CostPanel">
                        <TextBlock Text="Costo unitario" Style="{StaticResource FlexoCardLabelStyle}" />
                        <TextBox x:Name="UnitCostBox" Text="0" Style="{StaticResource FlexoFieldTextBoxStyle}" Margin="0,4,0,10" />
                    </StackPanel>

                    <TextBlock Text="Motivo" Style="{StaticResource FlexoCardLabelStyle}" />
                    <TextBox x:Name="ReasonBox" Style="{StaticResource FlexoFieldTextBoxStyle}"
                             TextWrapping="Wrap" AcceptsReturn="True" Height="50" Margin="0,4,0,10" />

                    <TextBlock x:Name="FormErrorText" Foreground="{StaticResource FlexoError}"
                               TextWrapping="Wrap" Visibility="Collapsed" Margin="0,0,0,8" />

                    <Button Style="{StaticResource FlexoPrimaryWideButtonStyle}" Click="OnRegistrarClick">
                        <TextBlock Text="REGISTRAR" Foreground="{StaticResource FlexoBlanco}" FontWeight="Bold" />
                    </Button>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
